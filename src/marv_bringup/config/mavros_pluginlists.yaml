# ArduPilot plugin list + Marv workarounds (MAVROS 2.14 companion plugin crash).
/**:
  ros__parameters:
    plugin_denylist:
      - actuator_control
      - ftp
      - hil
      - altitude
      - debug_value
      - image_pub
      - px4flow
      - vibration
      - vision_speed_estimate
      - wheel_odometry
      - companion_process_status
